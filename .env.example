# API base. Same origin in production builds; dev backend by default.
VITE_API_URL=http://localhost:8080
# Dev convenience only: subject id sent as X-Subject-ID when no session
# exists. Leave empty in real use; login owns the session afterwards.
VITE_DEV_SUBJECT_ID=
